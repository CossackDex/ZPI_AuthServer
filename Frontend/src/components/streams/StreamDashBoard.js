import React from "react";
import Navbar from "../Navbar";
import LogForm from "../LogForm";

export default () => {
    return (
        <div>
            <Navbar/>
        </div>
        
    );
  };
